services:
  app:
    build:
      context: .
    ports:
      - ":8000"
    environment:
      - NVIDIA_VISIBLE_DEVICES=${GPU_OPTION:-none}
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]